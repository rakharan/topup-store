# TODO — Audit Findings

## HIGH PRIORITY:
- [x] CSRF token store (`CSRFStore.tokens`) has no mutex — data race under concurrent load (file: internal/middleware/csrf.go)
- [x] WhatsApp bot `/notify` endpoint has no authentication — anyone can send arbitrary messages (file: whatsapp-bot/index.js)
- [x] Admin login form has no CSRF token — vulnerable to cross-site POST attacks (file: web/templates/admin.html)
- [x] Order creation fetch sends no `X-CSRF-Token` header — vulnerable to CSRF (file: web/templates/order.html)
- [x] Client-side XSS via `innerHTML` with unsanitized API data across all templates (file: web/templates/index.html, order.html, status.html, admin.html)
- [x] `Secure: true` on admin auth cookie breaks HTTP development — login fails locally (file: internal/handlers/pages.go)
- [x] Digiflazz API calls never check `resp.StatusCode` — 500/404 responses parsed as success (file: internal/services/topup.go)
- [x] `CreateQRIS` error silently swallowed — client gets 201 with empty payment URL (file: internal/handlers/orders.go)
- [x] `RetryOrder` allows retry on `processing` status but `ProcessOrder` only transitions from `paid` — admin gets false OK (file: internal/handlers/admin.go)
- [x] WhatsApp bot Dockerfile runs as root — should add non-root user (file: whatsapp-bot/Dockerfile)
- [x] Background goroutines have no `defer recover()` — panic crashes entire process (file: internal/handlers/webhooks.go, admin.go, orders.go, cmd/server/main.go)
- [x] `processTopupViaDigiflazz` ignores `UpdateStatus` errors — order stuck in `processing` on failure (file: internal/services/topup.go)
- [x] `ExistsBySKU` errors ignored in admin product create/update — allows duplicate SKUs on DB failure (file: internal/handlers/admin.go)
- [x] Migration 003 seed data duplicates on re-run — no `ON CONFLICT` clause (file: migrations/003_seed_products.sql)
- [x] `db/seed.go` uses `ON CONFLICT (sku)` but unique constraint not added until migration 007 — fails if run early (file: migrations/001_init.sql)
- [x] `go.sum` missing from repo — Docker build not reproducible (file: go.sum)

## MEDIUM PRIORITY:
- [x] Rate limiter trusts `X-Forwarded-For` without validation — easily spoofed (file: internal/middleware/ratelimit.go)
- [x] Midtrans webhook signature uses SHA-512 — verified correct per Midtrans docs (order_id + status_code + gross_amount + server_key) (file: internal/handlers/webhooks.go)
- [x] Digiflazz webhook signature verification skipped when secret is empty — accepts any payload (file: internal/handlers/webhooks.go)
- [ ] `http.FileServer` serves entire `web/static/` directory — no allowlist/blocklist (file: cmd/server/main.go)
- [x] CORS allows `*` when `ALLOWED_ORIGINS` is empty (file: internal/middleware/cors.go)
- [x] Missing `Content-Security-Policy` header (file: internal/middleware/security.go)
- [x] Missing `Strict-Transport-Security` (HSTS) header (file: internal/middleware/security.go)
- [x] Digiflazz webhook logs raw request body — may contain sensitive data (file: internal/handlers/webhooks.go)
- [x] WhatsApp bot Dockerfile has no healthcheck (file: whatsapp-bot/Dockerfile)
- [x] WhatsApp bot has no graceful shutdown handler (file: whatsapp-bot/index.js)
- [x] No `.dockerignore` in root — slow builds, potential secret inclusion (file: .dockerignore)
- [x] No `.dockerignore` in whatsapp-bot/ — `.env` baked into image (file: whatsapp-bot/.dockerignore)
- [x] PostgreSQL port 5432 exposed to host network — should be internal only (file: docker-compose.yml)
- [x] Hardcoded default PostgreSQL credentials (`topup`/`topup`) (file: docker-compose.yml)
- [x] Missing `/api/orders` list endpoint — `ListOrders` service exists but not exposed (file: cmd/server/main.go)
- [ ] No user-facing endpoint to cancel an order (file: internal/handlers/orders.go)
- [ ] No separate `/ready` health endpoint for readiness probes (file: cmd/server/main.go)
- [x] Log level hardcoded to `INFO` — no env var for DEBUG (file: cmd/server/main.go)
- [ ] `BasicAuth` middleware defined but never wired to any route (file: internal/middleware/auth.go)
- [ ] `Metrics` struct (non-Prometheus) defined but never instantiated (file: internal/middleware/metrics.go)
- [ ] `ListByStatus` repository method defined but never called (file: internal/repositories/order_repo.go)
- [ ] `PrometheusMetrics` handler outputs incomplete Prometheus format — no `_bucket` entries (file: internal/middleware/metrics.go)
- [x] Duplicate product seed data: migration 003 vs `db/seed.go` have completely different SKUs (file: migrations/003_seed_products.sql)
- [x] Migration 007 uses `ctid` for dedup — unstable across VACUUM (file: migrations/007_deduplicate_products.sql)
- [ ] No migration version tracking table — relies on `IF NOT EXISTS` idempotency (file: migrations/)
- [x] `whatsapp-bot/.env.example` missing `WHATSAPP_TOKEN`, `WHATSAPP_PHONE_NUMBER_ID`, `WHATSAPP_VERIFY_TOKEN` (file: whatsapp-bot/.env.example)
- [ ] `game_uid` lookup ambiguous for Mobile Legends — stores `UID|SERVER` but lookup receives them separately (file: internal/repositories/order_repo.go)
- [x] `rand.Read` error silently returns empty CSRF token — now returns HTTP 500 (file: internal/middleware/csrf.go)
- [x] Fire-and-forget goroutines use `context.Background()` — now use server root context, cancelled on shutdown (file: internal/handlers/webhooks.go, orders.go)
- [x] `os.Exit(1)` in server goroutine bypasses deferred cleanup — replaced with error channel + graceful shutdown (file: cmd/server/main.go)
- [x] CSRF token store is in-memory — replaced with PostgreSQL-backed store (migration 018, file: internal/middleware/csrf.go)
- [ ] Request body not drained on early JSON decode failure — connection reuse issues (file: internal/handlers/orders.go, admin.go)
- [ ] `json.NewEncoder(w).Encode()` errors never checked (file: internal/apperrors/errors.go)
- [ ] `strconv.Atoi` errors silently ignored in product handler (file: internal/handlers/products.go)

## LOW PRIORITY:
- [x] Missing favicon — `/static/favicon.ico` returns 404 (file: web/static/favicon.ico)
- [x] Dead file: `web/static/js/main.js` — not referenced by any template (file: web/static/js/main.js)
- [x] Dead file: `web/static/images/diamond.svg` — not referenced by any template (file: web/static/images/diamond.svg)
- [x] Duplicate `.focus-ring` CSS rule in `head.html` and `style.css` (file: web/templates/partials/head.html, web/static/css/style.css)
- [x] Duplicate `showToast` function in `toast.html` and `admin.html` (file: web/templates/partials/toast.html, web/templates/admin.html)
- [ ] Mobile menu toggle code copy-pasted in all 4 templates — should be extracted to shared JS (file: web/templates/*.html)
- [x] `document.getElementById('mobileMenuBtn')` has no null check in index, status, order templates (file: web/templates/index.html, status.html, order.html)
- [x] Confirmation modal missing `role="dialog"`, `aria-modal="true"`, `aria-labelledby` (file: web/templates/order.html)
- [x] No Escape key handler on confirmation modal (file: web/templates/order.html)
- [ ] Table headers missing `scope="col"` in admin tables (file: web/templates/admin.html)
- [x] Form inputs missing `aria-describedby` linking to hint text (file: web/templates/order.html)
- [ ] No skip-to-content link for keyboard users (file: web/templates/partials/nav.html)
- [x] `X-XSS-Protection` header is deprecated — removed (file: internal/middleware/security.go)
- [x] `X-Frame-Options: DENY` may be too restrictive — changed to `SAMEORIGIN` (file: internal/middleware/security.go)
- [x] AGENTS.md says WhatsApp bot uses Baileys — actual code uses Cloud API (file: AGENTS.md)
- [x] AGENTS.md missing `internal/repositories/`, `internal/apperrors/`, `internal/constants/`, `internal/retry/` from project structure (file: AGENTS.md)
- [x] AGENTS.md says Tailwind CSS (CDN) — actual code uses CLI build (file: AGENTS.md)
- [ ] Root `package.json` has placeholder test script (file: package.json)
- [ ] `make migrate` requires local `psql` — won't work on Windows without PostgreSQL tools (file: Makefile)
- [ ] `healthHandler` uses duck-typed `interface{}` — unconventional (file: cmd/server/main.go)
- [ ] Test files ignore `json.Decode` errors — assertions could silently pass (file: internal/handlers/handlers_test.go)
